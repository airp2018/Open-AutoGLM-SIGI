# 📱 AutoGLM ADB Keyboard 配置向导

> **适用场景**：为了在美团等混合架构 App 中实现可靠的文本输入，需要配置 ADB Keyboard 输入法。

---

## 🎯 配置目标

完成以下三个步骤后，AutoGLM 将能够在任何 App 中稳定输入文字：
1. ✅ 安装 ADB 工具
2. ✅ 授予 AutoGLM 系统权限
3. ✅ 启用 ADB Keyboard 输入法

**预计耗时**：首次配置约 10 分钟

---

## 📋 准备工作

### 需要准备的物品：
- ✅ 一台 Windows 电脑
- ✅ 一部安装了 AutoGLM 的 Android 手机
- ✅ 一根 USB 数据线（能传输数据，不是只能充电的线）

---

## 第一步：安装 ADB 工具（约 5 分钟）

### 方法一：使用 Google 官方 Platform Tools（推荐）

#### 1.1 下载 ADB 工具包

访问以下链接下载：
```
https://developer.android.com/tools/releases/platform-tools
```

或者使用国内镜像（速度更快）：
```
https://dl.google.com/android/repository/platform-tools-latest-windows.zip
```

#### 1.2 解压到固定位置

1. 下载完成后，你会得到一个 `platform-tools-latest-windows.zip` 文件
2. 右键点击 → 选择"解压到当前文件夹"
3. 建议解压到：`C:\platform-tools\`（方便后续使用）

#### 1.3 添加到系统环境变量（可选但推荐）

**为什么要做这一步？**  
这样你在任何位置打开 PowerShell 都能直接使用 `adb` 命令，不用每次都切换目录。

**操作步骤**：
1. 按 `Win + R` 键，输入 `sysdm.cpl`，回车
2. 点击"高级"选项卡 → 点击"环境变量"按钮
3. 在"系统变量"区域找到 `Path`，双击打开
4. 点击"新建"，输入：`C:\platform-tools`
5. 一路点击"确定"保存

**验证是否成功**：
1. 按 `Win + X`，选择"Windows PowerShell"
2. 输入 `adb version`，回车
3. 如果显示版本号（如 `Android Debug Bridge version 1.0.41`），说明成功！

---

### 方法二：使用便携版（无需安装）

如果你不想配置环境变量，可以直接在解压后的文件夹中使用：

1. 解压 `platform-tools-latest-windows.zip` 到任意位置（如桌面）
2. 进入 `platform-tools` 文件夹
3. 按住 `Shift` 键，在空白处右键，选择"在此处打开 PowerShell 窗口"
4. 现在可以直接使用 `.\adb` 命令了

---

## 第二步：连接手机并启用调试模式（约 2 分钟）

### 2.1 启用开发者选项

1. 打开手机的"设置"
2. 找到"关于手机"（通常在最底部）
3. 连续点击"版本号"7 次
4. 系统会提示"您已处于开发者模式"

### 2.2 启用 USB 调试

1. 返回"设置"主界面
2. 找到"系统"或"更多设置"
3. 进入"开发者选项"
4. 找到"USB 调试"，打开开关
5. 弹出警告对话框时，点击"确定"

### 2.3 连接手机到电脑

1. 用 USB 数据线连接手机和电脑
2. 手机会弹出"允许 USB 调试吗？"对话框
3. **勾选"始终允许使用这台计算机进行调试"**
4. 点击"允许"

### 2.4 验证连接

在 PowerShell 中输入：
```powershell
adb devices
```

**正确的输出示例**：
```
List of devices attached
VVY0223511012816    device
```

**如果显示 `unauthorized`**：
- 说明你没有在手机上点击"允许"
- 重新拔插数据线，再次点击"允许"

**如果显示 `no devices/emulators found`**：
- 检查数据线是否支持数据传输（换一根试试）
- 检查 USB 调试是否已开启
- 尝试重启 ADB：`adb kill-server` 然后 `adb start-server`

---

## 第三步：配置 AutoGLM 权限（约 1 分钟）

### 3.1 授予系统权限

在 PowerShell 中依次执行以下命令：

```powershell
# 授予修改系统设置的权限（用于切换输入法）
adb shell pm grant com.autoglm.helper android.permission.WRITE_SECURE_SETTINGS
```

**执行后没有任何输出是正常的**，说明授权成功。

### 3.2 启用 ADB Keyboard 输入法

```powershell
# 启用 ADB Keyboard（如果已安装）
adb shell ime enable com.android.adbkeyboard/.AdbIME
```

**可能的输出**：
- `Input method com.android.adbkeyboard/.AdbIME: already enabled` ✅ 已启用
- `Error: Unknown input method` ⚠️ 需要先安装 ADB Keyboard

### 3.3 安装 ADB Keyboard（如果尚未安装）

如果上一步提示 `Unknown input method`，需要先安装 ADB Keyboard：

**下载地址**：
```
https://github.com/senzhk/ADBKeyBoard/releases/download/v2.0/ADBKeyboard.apk
```

**安装方法**：
```powershell
# 方法 1：通过 ADB 安装
adb install ADBKeyboard.apk

# 方法 2：直接传到手机上点击安装
adb push ADBKeyboard.apk /sdcard/Download/
# 然后在手机的"文件管理"→"下载"中找到并安装
```

安装完成后，重新执行 3.2 的启用命令。

---

## 🎉 配置完成！验证是否成功

### 验证步骤：

1. 打开 AutoGLM App
2. 确保"检查无障碍权限"显示为绿色 ✅
3. 下达任务："打开美团，搜索肯德基"
4. 观察 AI 是否能够成功在搜索框中输入文字

### 预期行为：

当 AI 需要输入文字时：
- 输入法会短暂切换（可能看不到明显变化）
- 文字会自动出现在输入框中
- 输入法会自动切回你的常用输入法

---

## ❓ 常见问题

### Q1: 每次重启手机都需要重新配置吗？

**A**: 不需要。权限是永久的，除非你：
- 卸载了 AutoGLM App
- 恢复了手机出厂设置

### Q2: 每次使用 AutoGLM 都需要连接电脑吗？

**A**: 不需要。配置完成后，手机可以完全独立使用。只有在以下情况需要重新连接：
- 重新安装了 AutoGLM
- 权限被意外撤销

### Q3: ADB Keyboard 会影响我日常打字吗？

**A**: 不会。AutoGLM 只在需要时临时切换到 ADB Keyboard，输入完成后会立即切回你的常用输入法。

### Q4: 为什么不能在 App 内直接授权？

**A**: `WRITE_SECURE_SETTINGS` 是 Android 系统的高级权限，出于安全考虑，只能通过 ADB 或系统签名授予。这是 Google 的安全策略，所有 App 都必须遵守。

### Q5: 我的电脑是 Mac/Linux，怎么办？

**Mac 用户**：
```bash
# 安装 ADB（使用 Homebrew）
brew install android-platform-tools

# 后续命令与 Windows 相同
adb shell pm grant com.autoglm.helper android.permission.WRITE_SECURE_SETTINGS
adb shell ime enable com.android.adbkeyboard/.AdbIME
```

**Linux 用户**：
```bash
# Ubuntu/Debian
sudo apt-get install android-tools-adb

# 后续命令与 Windows 相同
adb shell pm grant com.autoglm.helper android.permission.WRITE_SECURE_SETTINGS
adb shell ime enable com.android.adbkeyboard/.AdbIME
```

---

## 🔧 故障排除

### 问题：`adb` 命令无法识别

**解决方法**：
1. 确认已下载并解压 platform-tools
2. 如果没有配置环境变量，需要在 platform-tools 文件夹内打开 PowerShell
3. 或者使用完整路径：`C:\platform-tools\adb.exe devices`

### 问题：手机连接后显示 `offline`

**解决方法**：
1. 拔掉数据线重新插
2. 在手机上重新授权 USB 调试
3. 重启 ADB 服务：
   ```powershell
   adb kill-server
   adb start-server
   ```

### 问题：权限授予后仍然无法输入

**检查清单**：
1. ✅ 确认 ADB Keyboard 已安装
2. ✅ 确认 ADB Keyboard 已启用（`adb shell ime list` 查看）
3. ✅ 确认无障碍权限已开启
4. ✅ 尝试重启 AutoGLM App

---

## 📞 获取帮助

如果遇到本文档未覆盖的问题，可以：
1. 查看 AutoGLM 项目的 GitHub Issues
2. 在社区论坛发帖求助
3. 提供详细的错误信息和日志

---

## 📚 附录：一键配置脚本

如果你已经安装了 ADB 并连接了手机，可以使用以下脚本一键完成配置。

**创建方法**：
1. 新建一个文本文件
2. 将以下内容复制进去
3. 保存为 `AutoGLM-Setup.bat`
4. 双击运行

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo   AutoGLM ADB Keyboard 一键配置工具
echo ========================================
echo.
echo [1/3] 检测手机连接...
adb devices
echo.
echo [2/3] 授予系统权限...
adb shell pm grant com.autoglm.helper android.permission.WRITE_SECURE_SETTINGS
if %errorlevel% equ 0 (
    echo ✅ 权限授予成功
) else (
    echo ❌ 权限授予失败，请检查手机连接
    pause
    exit /b 1
)
echo.
echo [3/3] 启用 ADB Keyboard...
adb shell ime enable com.android.adbkeyboard/.AdbIME
if %errorlevel% equ 0 (
    echo ✅ ADB Keyboard 已启用
) else (
    echo ⚠️  ADB Keyboard 可能未安装
    echo    请先安装 ADBKeyboard.apk
)
echo.
echo ========================================
echo   配置完成！
echo   现在可以关闭此窗口并使用 AutoGLM
echo ========================================
pause
```

---

**最后更新时间**：2025-12-20  
**文档版本**：v1.0
