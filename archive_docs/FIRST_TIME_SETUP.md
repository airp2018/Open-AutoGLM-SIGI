# 📱 AutoGLM 首次使用完整配置指南

> **适用人群**：第一次安装 AutoGLM 的用户  
> **预计耗时**：15-20 分钟（含 ADB 安装）

---

## 🎯 配置目标

完成以下配置后，AutoGLM 将能够完全自主地控制手机、执行任务：

1. ✅ **安装 AutoGLM APK**
2. ✅ **开启无障碍权限**（必须，否则无法使用）
3. ✅ **配置 ADB Keyboard**（可选，但强烈推荐）

---

## 第一部分：安装与基础配置（必须完成）

### 步骤 1：安装 AutoGLM APK

1. 将 `AutoGLM.apk` 文件传输到手机
   - 方法 1：通过数据线复制到手机
   - 方法 2：通过微信/QQ 发送到手机
   - 方法 3：通过网盘下载到手机

2. 在手机上找到 APK 文件，点击安装
   - 如果提示"未知来源"，需要允许安装
   - 华为手机：设置 → 安全 → 更多安全设置 → 允许安装未知应用
   - 小米手机：设置 → 隐私保护 → 特殊权限 → 安装未知应用
   - 其他品牌：设置 → 应用 → 特殊访问权限 → 安装未知应用

3. 安装完成后，打开 AutoGLM

---

### 步骤 2：开启无障碍权限（核心步骤）

**为什么必须开启？**  
无障碍权限允许 AutoGLM 代替你的手指操作手机（点击、滑动、输入等）。没有这个权限，AutoGLM 完全无法工作。

#### 2.1 在 AutoGLM 内点击引导按钮

1. 打开 AutoGLM App
2. 你会看到一个红色的按钮："检查无障碍权限"
3. 点击这个按钮

#### 2.2 系统会自动跳转到设置页面

**不同品牌手机的路径可能略有不同**：

**华为/荣耀手机**：
```
设置 → 辅助功能 → 无障碍 → 已下载的服务 → AutoGLM → 打开开关
```

**小米手机**：
```
设置 → 更多设置 → 无障碍 → 已安装的服务 → AutoGLM → 打开开关
```

**OPPO/一加手机**：
```
设置 → 其他设置 → 无障碍 → 已下载的服务 → AutoGLM → 打开开关
```

**vivo 手机**：
```
设置 → 快捷与辅助 → 无障碍 → 已安装的服务 → AutoGLM → 打开开关
```

**三星手机**：
```
设置 → 辅助功能 → 已安装的服务 → AutoGLM → 打开开关
```

**原生 Android**：
```
设置 → 无障碍 → 已下载的服务 → AutoGLM → 使用 AutoGLM → 打开开关
```

#### 2.3 确认权限已开启

1. 系统会弹出警告对话框："AutoGLM 将能够..."
2. **点击"允许"或"确定"**
3. 返回 AutoGLM App
4. 如果"检查无障碍权限"按钮变成**绿色** ✅，说明成功！

---

### 🎉 基础配置完成！

现在你可以尝试简单的任务了：
- "打开微信"
- "打开设置"
- "返回桌面"

**但是**，如果你想让 AutoGLM 在美团、淘宝等复杂 App 中输入文字，还需要完成第二部分的配置。

---

## 第二部分：ADB Keyboard 配置（强烈推荐）

### 为什么需要这一步？

某些 App（如美团、淘宝、12306）使用了特殊的输入框技术，普通的无障碍服务无法可靠地输入文字。ADB Keyboard 可以解决这个问题。

**如果你只用 AutoGLM 控制简单的 App（如微信、设置），可以跳过这一步。**

---

### 准备工作

你需要：
- ✅ 一台 Windows/Mac/Linux 电脑
- ✅ 一根 USB 数据线（能传输数据）
- ✅ 10 分钟时间

---

### 步骤 3：安装 ADB 工具（电脑端）

#### Windows 用户：

**3.1 下载 ADB 工具包**

访问以下链接（选择一个）：
- 官方地址：https://developer.android.com/tools/releases/platform-tools
- 国内镜像：https://dl.google.com/android/repository/platform-tools-latest-windows.zip

**3.2 解压到固定位置**

1. 下载完成后得到 `platform-tools-latest-windows.zip`
2. 右键 → 解压到：`C:\adb\`（或任意你记得住的位置）

**3.3 测试 ADB 是否可用**

1. 进入 `C:\adb\platform-tools\` 文件夹
2. 按住 `Shift` 键，在空白处右键
3. 选择"在此处打开 PowerShell 窗口"
4. 输入：`.\adb version`
5. 如果显示版本号，说明成功！

#### Mac 用户：

```bash
# 使用 Homebrew 安装
brew install android-platform-tools

# 验证安装
adb version
```

#### Linux 用户：

```bash
# Ubuntu/Debian
sudo apt-get install android-tools-adb

# 验证安装
adb version
```

---

### 步骤 4：连接手机并启用 USB 调试

#### 4.1 开启开发者选项

1. 打开手机"设置"
2. 找到"关于手机"（通常在最底部）
3. **连续点击"版本号"7 次**
4. 系统提示"您已处于开发者模式"

#### 4.2 启用 USB 调试

1. 返回"设置"主界面
2. 找到"系统"或"更多设置"
3. 进入"开发者选项"
4. 找到"USB 调试"，**打开开关**
5. 弹出警告时点击"确定"

#### 4.3 连接手机到电脑

1. 用 USB 数据线连接手机和电脑
2. 手机会弹出"允许 USB 调试吗？"
3. **勾选"始终允许使用这台计算机进行调试"**
4. 点击"允许"

#### 4.4 验证连接

在 PowerShell（或终端）中输入：
```powershell
adb devices
```

**正确的输出**：
```
List of devices attached
ABC123456789    device
```

**如果显示 `unauthorized`**：
- 重新在手机上点击"允许"

**如果显示 `no devices`**：
- 检查数据线是否支持数据传输
- 检查 USB 调试是否已开启
- 尝试换个 USB 接口

---

### 步骤 5：安装 ADB Keyboard（手机端）

#### 5.1 下载 ADB Keyboard APK

下载地址：
```
https://github.com/senzhk/ADBKeyBoard/releases/download/v2.0/ADBKeyboard.apk
```

#### 5.2 安装到手机

**方法 1：通过 ADB 安装（推荐）**
```powershell
adb install ADBKeyboard.apk
```

**方法 2：手动安装**
1. 将 APK 传到手机
2. 在手机上点击安装

---

### 步骤 6：配置 AutoGLM 权限（电脑端）

#### 💡 这一步在做什么？

**简单来说**：给 AutoGLM 一个"临时管理员"权限，让它能够：
1. **自动切换输入法**：从你的常用输入法（如微信键盘）临时切换到 ADB Keyboard
2. **输入完成后切回来**：自动恢复你原来的输入法

**为什么需要这个权限？**
- 美团、淘宝等 App 的输入框很"刁钻"，普通方法打不进去字
- ADB Keyboard 是一种特殊的输入法，可以绕过这些限制
- 但是切换输入法需要系统级权限，所以必须通过电脑授权

**类比**：
- 就像你给朋友临时授权，让他能帮你切换手机的输入法
- 他用完后会立刻切回你原来的输入法
- 你的日常使用完全不受影响

**安全性**：
- 这个权限只能修改输入法设置，不能访问你的隐私数据
- AutoGLM 是开源软件，代码完全透明
- 你可以随时卸载 App 来撤销权限

---

#### 6.1 确认设备连接

首先执行：
```powershell
adb devices
```

**如果只显示一台设备**（推荐情况）：
```
List of devices attached
ABC123456789    device
```
→ 直接使用下面的**简化命令**

**如果显示多台设备**：
```
List of devices attached
ABC123456789    device
XYZ987654321    device
```
→ 需要使用**完整命令**（指定设备序列号）

#### 6.2 执行授权命令

**情况 A：只连接了一台手机（大多数情况）**

在 PowerShell（或终端）中依次执行：

```powershell
# 1. 授予 AutoGLM 修改系统设置的权限
adb shell pm grant com.autoglm.helper android.permission.WRITE_SECURE_SETTINGS

# 2. 启用 ADB Keyboard 输入法
adb shell ime enable com.android.adbkeyboard/.AdbIME
```

**情况 B：连接了多台设备**

需要在命令中指定设备序列号（从 `adb devices` 的输出中复制）：

```powershell
# 假设你的设备序列号是 ABC123456789
# 1. 授予权限
adb -s ABC123456789 shell pm grant com.autoglm.helper android.permission.WRITE_SECURE_SETTINGS

# 2. 启用输入法
adb -s ABC123456789 shell ime enable com.android.adbkeyboard/.AdbIME
```

**💡 提示**：将 `ABC123456789` 替换成你自己手机的序列号。

#### 6.3 验证配置

**执行后没有任何输出是正常的**，说明成功。

如果想确认权限是否授予成功，可以执行：
```powershell
# 查看 AutoGLM 的权限列表
adb shell dumpsys package com.autoglm.helper | findstr "WRITE_SECURE_SETTINGS"
```

如果输出包含 `granted=true`，说明权限已成功授予。

---

### 🎉 全部配置完成！

现在你可以尝试复杂的任务了：
- "打开美团，搜索肯德基，买一份香辣鸡翅"
- "打开淘宝，搜索 iPad"
- "打开 12306，查询北京到上海的高铁"

---

## ❓ 常见问题

### Q1: 无障碍权限会自动关闭怎么办？

**A**: 某些手机（如小米、OPPO）的省电模式会自动关闭无障碍服务。解决方法：
1. 将 AutoGLM 加入"省电白名单"
2. 允许 AutoGLM 后台运行
3. 关闭"自动优化"功能

**小米手机**：
```
设置 → 应用设置 → 应用管理 → AutoGLM → 省电策略 → 无限制
```

**华为手机**：
```
设置 → 应用 → 应用启动管理 → AutoGLM → 手动管理 → 全部打开
```

### Q2: 每次重启手机都要重新配置吗？

**A**: 
- **无障碍权限**：不需要，但某些手机可能会自动关闭（见 Q1）
- **ADB Keyboard 权限**：不需要，配置一次永久有效

### Q3: 不连接电脑能用吗？

**A**: 
- **基础功能**（点击、滑动）：可以，只需要无障碍权限
- **高级输入**（美团等 App）：配置完成后可以，但首次配置需要电脑

### Q4: 卸载重装后需要重新配置吗？

**A**: 
- **无障碍权限**：需要重新开启
- **ADB Keyboard 权限**：需要重新授予

### Q5: 为什么有些 App 无法输入？

**A**: 
如果你已经完成了 ADB Keyboard 配置但仍然无法输入，可能的原因：
1. ADB Keyboard 未正确安装（执行 `adb shell ime list` 检查）
2. 权限未正确授予（重新执行步骤 6）
3. 该 App 有特殊的输入保护（极少数情况）

---

## 🔧 故障排除

### 问题：点击"检查无障碍权限"没反应

**解决方法**：
1. 手动进入系统设置
2. 按照步骤 2.2 的路径找到无障碍设置
3. 手动开启 AutoGLM 的无障碍权限

### 问题：无障碍权限开启后仍然是红色

**解决方法**：
1. 完全关闭 AutoGLM App（从后台清除）
2. 重新打开 AutoGLM
3. 如果仍然不行，重启手机

### 问题：ADB 命令提示"设备未找到"

**解决方法**：
1. 确认 USB 调试已开启
2. 重新拔插数据线
3. 在手机上重新授权 USB 调试
4. 重启 ADB 服务：
   ```powershell
   adb kill-server
   adb start-server
   adb devices
   ```

### 问题：授权命令执行失败

**可能的错误信息**：
```
Error: Unknown package: com.autoglm.helper
```

**解决方法**：
- 说明 AutoGLM 未正确安装
- 检查 APK 是否安装成功
- 尝试重新安装

---

## 📦 一键配置脚本（高级用户）

如果你熟悉命令行，可以使用以下脚本一键完成 ADB Keyboard 配置。

**创建 `AutoGLM-Setup.bat` 文件**（Windows）：

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo   AutoGLM 一键配置工具
echo ========================================
echo.
echo [1/4] 检测手机连接...
adb devices
if %errorlevel% neq 0 (
    echo ❌ ADB 未安装或手机未连接
    echo    请先完成步骤 3-4
    pause
    exit /b 1
)
echo.
echo [2/4] 安装 ADB Keyboard...
if exist ADBKeyboard.apk (
    adb install -r ADBKeyboard.apk
    echo ✅ ADB Keyboard 安装完成
) else (
    echo ⚠️  未找到 ADBKeyboard.apk
    echo    请将 APK 放在脚本同一目录
)
echo.
echo [3/4] 授予系统权限...
adb shell pm grant com.autoglm.helper android.permission.WRITE_SECURE_SETTINGS
if %errorlevel% equ 0 (
    echo ✅ 权限授予成功
) else (
    echo ❌ 权限授予失败
    pause
    exit /b 1
)
echo.
echo [4/4] 启用 ADB Keyboard...
adb shell ime enable com.android.adbkeyboard/.AdbIME
echo ✅ ADB Keyboard 已启用
echo.
echo ========================================
echo   配置完成！
echo   现在可以使用 AutoGLM 的全部功能
echo ========================================
pause
```

**使用方法**：
1. 将上述内容保存为 `AutoGLM-Setup.bat`
2. 将 `ADBKeyboard.apk` 放在同一目录
3. 双击运行

---

## 📞 需要帮助？

如果遇到问题：
1. 仔细阅读本文档的"常见问题"和"故障排除"部分
2. 查看 AutoGLM 的 GitHub Issues
3. 在社区论坛发帖求助（提供详细的错误信息）

---

**文档版本**：v1.0  
**最后更新**：2025-12-20  
**适用版本**：AutoGLM v1.1+
